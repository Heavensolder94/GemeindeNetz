<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="theme-color" content="#f4f6fb" />
  <title>GemeindeNetz – Registrierung, Login & Dashboard</title>
  <style>
    :root {
      --bg: #f3f5f8;
      --surface: rgba(255,255,255,.78);
      --surface-solid: #fff;
      --surface-soft: rgba(255,255,255,.64);
      --text: #0f1115;
      --muted: #6d727c;
      --line: rgba(15,17,21,.08);
      --line-strong: rgba(15,17,21,.14);
      --primary: #246bfd;
      --primary-2: #7658f2;
      --accent: #16b88d;
      --warning: #f5a524;
      --danger: #e45d75;
      --success: #18a879;
      --shadow-sm: 0 12px 34px rgba(35,42,60,.08);
      --shadow-lg: 0 28px 80px rgba(35,42,60,.16);
      --radius: 24px;
      --radius-lg: 34px;
      --container: min(1180px, calc(100% - 32px));
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }
    html { scroll-behavior: smooth; }
    body {
      min-height: 100vh;
      overflow-x: hidden;
      color: var(--text);
      background:
        radial-gradient(circle at 8% 0%, rgba(36,107,253,.14), transparent 26%),
        radial-gradient(circle at 92% 4%, rgba(118,88,242,.12), transparent 25%),
        radial-gradient(circle at 50% 100%, rgba(22,184,141,.10), transparent 28%),
        var(--bg);
      font-family: -apple-system, BlinkMacSystemFont, "SF Pro Display", "Segoe UI",
        Roboto, Helvetica, Arial, sans-serif;
      -webkit-font-smoothing: antialiased;
    }

    body::before {
      content: "";
      position: fixed;
      inset: 0;
      z-index: -2;
      pointer-events: none;
      background-image:
        linear-gradient(rgba(255,255,255,.22) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255,255,255,.22) 1px, transparent 1px);
      background-size: 44px 44px;
      mask-image: linear-gradient(to bottom, black, transparent 78%);
      opacity: .22;
    }

    a { color: inherit; text-decoration: none; }
    button, input, select, textarea { font: inherit; }
    button { cursor: pointer; }
    img { max-width: 100%; display: block; }

    .container { width: var(--container); margin: 0 auto; }
    .hidden { display: none !important; }

    .glass {
      background: var(--surface);
      border: 1px solid rgba(255,255,255,.9);
      box-shadow: var(--shadow-sm);
      backdrop-filter: blur(24px) saturate(150%);
      -webkit-backdrop-filter: blur(24px) saturate(150%);
    }

    .nav-shell {
      position: sticky;
      top: 0;
      z-index: 70;
      padding: 12px 0 0;
    }

    .nav {
      width: var(--container);
      height: 68px;
      margin: 0 auto;
      padding: 0 14px 0 18px;
      border-radius: 22px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      transition: .25s ease;
    }

    .brand {
      display: inline-flex;
      align-items: center;
      gap: 11px;
      font-size: 1.08rem;
      font-weight: 800;
      letter-spacing: -.035em;
    }

    .brand-mark {
      width: 39px;
      height: 39px;
      border-radius: 13px;
      display: grid;
      place-items: center;
      color: #fff;
      background:
        linear-gradient(145deg, rgba(255,255,255,.34), transparent 48%),
        linear-gradient(135deg, var(--primary), var(--primary-2));
      box-shadow: 0 12px 24px rgba(55,92,225,.26);
    }

    .brand-mark svg { width: 22px; height: 22px; }

    .nav-links {
      display: flex;
      align-items: center;
      gap: 7px;
      color: #555a63;
      font-size: .92rem;
      font-weight: 650;
    }

    .nav-link {
      padding: 10px 13px;
      border: 0;
      border-radius: 12px;
      background: transparent;
      color: inherit;
      transition: .2s ease;
    }

    .nav-link:hover,
    .nav-link.active {
      color: var(--text);
      background: rgba(255,255,255,.72);
    }

    .nav-actions {
      display: flex;
      align-items: center;
      gap: 9px;
    }

    .btn {
      min-height: 44px;
      padding: 0 18px;
      border: 0;
      border-radius: 14px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 9px;
      white-space: nowrap;
      font-weight: 760;
      letter-spacing: -.015em;
      transition: transform .22s ease, box-shadow .22s ease, background .22s ease;
    }

    .btn:hover { transform: translateY(-2px); }

    .btn-primary {
      color: #fff;
      background:
        linear-gradient(145deg, rgba(255,255,255,.18), transparent 46%),
        linear-gradient(135deg, var(--primary), var(--primary-2));
      box-shadow: 0 12px 28px rgba(52,92,226,.26);
    }

    .btn-ghost {
      color: var(--text);
      background: rgba(255,255,255,.72);
      border: 1px solid var(--line);
    }

    .btn-danger {
      color: #b33a50;
      background: rgba(228,93,117,.10);
      border: 1px solid rgba(228,93,117,.16);
    }

    .btn-small {
      min-height: 38px;
      padding: 0 13px;
      border-radius: 12px;
      font-size: .84rem;
    }

    .avatar-button {
      width: 44px;
      height: 44px;
      padding: 0;
      border-radius: 14px;
      border: 0;
      display: grid;
      place-items: center;
      color: #fff;
      font-weight: 850;
      letter-spacing: -.04em;
      background: linear-gradient(135deg, var(--primary), var(--primary-2));
      box-shadow: 0 12px 24px rgba(52,92,226,.22);
    }

    .view {
      display: none;
      min-height: calc(100vh - 80px);
      animation: viewIn .35s ease;
    }

    .view.active { display: block; }

    @keyframes viewIn {
      from { opacity: 0; transform: translateY(8px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* HOME */
    .hero {
      padding: 86px 0 70px;
    }

    .hero-grid {
      display: grid;
      grid-template-columns: 1.02fr .98fr;
      gap: 48px;
      align-items: center;
    }

    .eyebrow {
      display: inline-flex;
      align-items: center;
      gap: 9px;
      padding: 8px 12px;
      margin-bottom: 22px;
      border-radius: 999px;
      color: #42527c;
      background: rgba(255,255,255,.72);
      border: 1px solid rgba(255,255,255,.9);
      box-shadow: 0 8px 20px rgba(36,43,61,.08);
      font-size: .84rem;
      font-weight: 760;
    }

    .eyebrow-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: linear-gradient(135deg, var(--accent), #6be0bf);
      box-shadow: 0 0 0 5px rgba(22,184,141,.12);
    }

    h1 {
      max-width: 820px;
      font-size: clamp(3rem, 6vw, 5.35rem);
      line-height: .96;
      letter-spacing: -.07em;
    }

    .gradient-text {
      color: transparent;
      background: linear-gradient(105deg, #1d66f2 8%, #7859f0 58%, #14a77f 100%);
      -webkit-background-clip: text;
      background-clip: text;
    }

    .hero-copy {
      max-width: 680px;
      margin: 27px 0 30px;
      color: var(--muted);
      font-size: clamp(1.04rem, 2vw, 1.22rem);
      line